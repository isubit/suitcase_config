<?php

/**
 * Implements hook_enable()
 */
function suitcase_ckeditor_overrides_enable() {
  db_update('system')
    ->fields(array('weight' => 100000))
    ->condition('type', 'module')
    ->condition('name', 'suitcase_ckeditor_overrides')
    ->execute();
}